{
  "owner_qq": {
    "type": "string",
    "description": "主人身份标识(openid)",
    "hint": "qq_official 不返回QQ号，这里填你的 openid（形如 B3DAEBEB...）。触发一次「更新视频库」被拒时，提示语会显示你的 openid，照抄填入即可",
    "default": ""
  },
  "music_image": {
    "type": "string",
    "description": "大狗听音乐的固定图片文件名",
    "hint": "放在 images/ 目录下的文件名（默认 listen.png），发送视频时先发这张图",
    "default": "listen.png"
  }
}
