{
  "owner_qq": {
    "type": "string",
    "description": "主人QQ号",
    "hint": "你的QQ号，只有这个号可以触发「大狗 更新视频库」",
    "default": ""
  },
  "music_image": {
    "type": "string",
    "description": "大狗听音乐的固定图片文件名",
    "hint": "放在 images/ 目录下的文件名（默认 listen.png），发送视频时先发这张图",
    "default": "listen.png"
  }
}
